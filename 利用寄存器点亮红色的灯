int main()
{
	//我想实现其他颜色的灯去点亮
	//例如：红色的灯
	
	//还应该设置时钟RCC RCC处于AHB总线上，地址0x4002 1000  APB2偏移地址：0x18
	//处于位3上
	* (unsigned int*)0x40021018 |= ( 1<<3 );
	
	//GPIO口应该设置为输出模式，LED灯为低位，采用端口配置低寄存器
	//GPIO端口B的地址0X4001 0C00 偏移地址：0x00
	//采用推挽输出模式，0001的结构
	* (unsigned int*)0X40010C00 |= (1<<(4*5));
	
	//红色的灯对应的PB5,想实现他点亮，应该置PB5为0
	//找到GPIO端口B的地址0X4001 0C00，地址偏移：0Ch
	* (unsigned int*)0X40010C0C &=~(1<<5);
}

//在32的起始文件中，刚开始执行时，会先执行Reset_Handler，其中SystemInit是需要插入的，现先自定义个空函数让其正常运行 
void SystemInit()
{}
	
