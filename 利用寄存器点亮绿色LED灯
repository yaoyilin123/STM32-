#include "stm32f10x.h"   


int main()
{
	//时钟就像心脏 没有时钟系统不能工作
	//所以应该将RCC系统置1
	//PB是位3，
	//RCC系统在AHB总线上，地址为0x4002 1000，偏移地址0x18
		*(unsigned int*)0X40021018 |=(1<<3);
	
	
	//GPIO端口B既可以做输入也可以做输出，所以我应该在前侧表明此时，做输入还是输出。
	//由于LED灯的几个端口都是低位，所以在低寄存器中配置
	//CNF0和MODE0是一组， 根据表格我应该置其为 00 01 （选择推挽输出）
	//此处偏移0
	*(unsigned int*)0X40010C00 |=(1<<(4*0));
	
	//我想让绿色的led亮，与MCU相连的点是PB0，我需要将其置于0
	//GPIO端口B的地址为0X4001 0C00
	//我需要让B口的DOR为0，地址偏移为0Ch,则在端口B的位置上加0C
	*(unsigned int*)0X40010C0C &= ~(1<<0);
	
}

//在32的起始文件中，刚开始执行时，会先执行Reset_Handler，其中SystemInit是需要插入的，现先自定义个空函数让其正常运行 
void SystemInit()
{}
	
